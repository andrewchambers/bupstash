
The write log could to reference/multiplex on the repository id if we want to allow multiple repos.

We should use libsodium and optimise our usage authentication to prevent excessive 
hashing.

Design a storage engine plugin protocol/interface.