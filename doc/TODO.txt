XXX TODO

The write log needs to reference/multiplex on the repository id if we want to allow multiple repos.

The write log should probably allow more than a single a/b cycle. We could cap by some counter instead.