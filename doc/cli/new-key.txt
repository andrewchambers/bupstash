bupstash new-key [OPTIONS]

Create a new key, readable by only the creating user.

Keep this key private and secure as it is needed to read any
data put in a repository using this key.

Examples:
  $ bupstash new-key -o ./backups.key
