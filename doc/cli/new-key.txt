bupstash new-master-key

Description:
  Create a new key readable by only the creating user.

  Keep this key private and secure as it is needed to read any archived
  data sent using send keys derived from the master key.

Quick examples:
  $ bupstash new-key -o ./backups.key

Usage:
  bupstash new-key [OPTIONS]